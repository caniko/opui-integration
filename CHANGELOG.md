# Changelog

## 0.1.0-rc.1

- Profile-aware certification requires every locally owned RC1 gate to pass and
  permits only explicit external platform, hardware, and signing blockers.
- Automated public-API and semver checks preserve the alpha.3 surface.
- Release-mode graphical stress and performance distributions cover repeated
  reload, resize, mount/unmount, changed-node work, and peak RSS budgets.
- Paired independent clean capsules produce identical crate and SBOM manifests,
  with unsigned provenance bound to exact repositories and toolchains.

## 0.1.0-alpha.3

- Canonical Veritasium includes owner-tested pointer cancellation at exact revision
  `7cd58438d458d3e701c32d5d5ae0c7b1f70a2bc0`.
- Package preflight compiles a fresh downstream consumer from unpacked schema and
  runtime `.crate` artifacts without path dependencies.
- Runtime probes retain complete accessibility snapshots with stable runtime IDs.
- Reconciliation stress covers 1,000 reloads with stable identity and entity count.
- The release aggregate retains a three-viewport windowed Weston/Vulkan matrix.

## 0.1.0-alpha.2

- `just certify-case CASE SIZE` writes `provenance.json` + package hashes.
- Capture waits on `ScreenshotCaptured` + two identical post-layout snapshots.
- Focused cases cover native core, fluid layout, typography, raster fallback,
  gradients, diagnostics/hot reload, and the kitchen sink.
- `just certify-release` aggregates lock, package, loader, visual, and case gates.
- OpenPencil's Nix flake supplies the reference renderer and raster dependencies.
- Visual budget still 18 / 220. Ellipse stays raster-only.
- Bevy resolves from the exact Veritasium 0.19.1 revision used for visual certification.
- Publishable schema and runtime crates move together to `0.1.0-alpha.2`.
- Clean release capsules isolate operator dirt, build both Nix exporters, run
  strict locks and the full viewport matrix, and atomically retain evidence.
- Package preflight validates the runtime `.crate` against the exact packaged
  schema without publishing either crate.
- Container fill items retain intrinsic basis while text/image leaves remain
  evenly weighted, closing gradient parity without regressing typography.
- The clean aggregate passes every release-blocking case and records an overall
  `RELEASE AS EXPERIMENTAL` because two non-blocking semantic snapshots await
  separate review.

## 0.1.0-alpha.1

- Public path: `op export --format opui` → `opui check` → `bevy_openpencil`.
- Visual harness: `bevy-shot --scene control|opui`.
- Raster-native lane exists (`just export-raster`) but the earlier local Linux environment could not link it (`freetype`).
